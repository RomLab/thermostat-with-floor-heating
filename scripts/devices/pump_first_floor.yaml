turn_off_pump_first_floor:
  sequence:
    - service: switch.turn_off
      data:
        entity_id: switch.pump_first_floor

turn_on_pump_first_floor:
  sequence:
    - service: switch.turn_on
      data:
        entity_id: switch.pump_first_floor