turn_on_actuator_corner_room:
  alias: 'Turn on actuators for a corner room'
  description: 'Turn on actuators for a corner room'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.corner_room', 'hvac_action', 'heating') }}"
    - service: input_number.set_value
      data:
        entity_id: 
          - input_number.thermo_actuator_corner_room
        value: '100'

turn_off_actuator_corner_room:
  alias: 'Turn off actuators for a corner room'
  description: 'Turn off actuators for a corner room'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.corner_room', 'hvac_action', 'idle') or  is_state_attr('climate.corner_room', 'hvac_action', 'off')}}"
    - service: input_number.set_value
      data:
        entity_id: 
          - input_number.thermo_actuator_corner_room
        value: '0'