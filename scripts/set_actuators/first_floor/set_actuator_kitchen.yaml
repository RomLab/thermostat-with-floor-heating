turn_on_actuator_first_floor_kitchen:
  alias: 'Turn on actuators for a kitchen in a first floor'
  description: 'Turn on actuators for a kitchen in a first floor'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.first_floor_kitchen', 'hvac_action', 'heating') }}"
    - service: input_number.set_value
      data:
        entity_id: 
          - input_number.thermo_actuator_first_floor_kitchen_windows
          - input_number.thermo_actuator_first_floor_kitchen_door
        value: '100'

turn_off_actuator_first_floor_kitchen:
  alias: 'Turn off actuators for a kitchen in a first floor'
  description: 'Turn off actuators for a kitchen in a first floor'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.first_floor_kitchen', 'hvac_action', 'idle') or  is_state_attr('climate.first_floor_kitchen', 'hvac_action', 'off')}}"
    - service: input_number.set_value
      data:
        entity_id: 
          - input_number.thermo_actuator_first_floor_kitchen_windows
          - input_number.thermo_actuator_first_floor_kitchen_door
        value: '0'