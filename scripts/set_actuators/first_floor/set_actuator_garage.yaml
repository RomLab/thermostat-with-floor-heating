turn_on_actuator_first_floor_garage:
  alias: 'Turn on actuators for a garage in a first floor'
  description: 'Turn on actuators for a garage in a first floor'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.first_floor_garage', 'hvac_action', 'heating') }}"
    - service: input_number.set_value
      data:
        entity_id: 
          - input_number.thermo_actuator_first_floor_garage_1
          - input_number.thermo_actuator_first_floor_garage_2
        value: '100'

turn_off_actuator_first_floor_garage:
  alias: 'Turn off actuators for a garage in a first floor'
  description: 'Turn off actuators for a garage in a first floor'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.first_floor_garage', 'hvac_action', 'idle') or  is_state_attr('climate.first_floor_garage', 'hvac_action', 'off')}}"
    - service: input_number.set_value
      data:
        entity_id: 
          - input_number.thermo_actuator_first_floor_garage_1
          - input_number.thermo_actuator_first_floor_garage_2
        value: '0'