turn_on_actuator_first_floor_office:
  alias: 'Turn on actuators for a office in a first floor'
  description: 'Turn on actuators for a office in a first floor'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.first_floor_office', 'hvac_action', 'heating') }}"
    - service: input_boolean.turn_on
      data:
        entity_id: 
          - input_boolean.thermo_actuator_first_floor_office

turn_off_actuator_first_floor_office:
  alias: 'Turn off actuators for a office in a first floor'
  description: 'Turn off actuators for a office in a first floor'
  sequence:
    - condition: template
      value_template: "{{ is_state_attr('climate.first_floor_office', 'hvac_action', 'idle') or  is_state_attr('climate.first_floor_office', 'hvac_action', 'off')}}"
    - service: input_boolean.turn_off
      data:
        entity_id: 
          - input_boolean.thermo_actuator_first_floor_office